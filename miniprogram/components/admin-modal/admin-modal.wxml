<view class='mask' wx:if='{{show}}' bindtap='clickMask'></view>
<view class='modal' wx:if='{{show}}'>
  <image src="../../images/component_bg.jpg" class="bg"></image>
  <view class="title">
    管理员登入
  </view>
  <view class="question-content">
    <text class="hint">管理员账号</text><input bindinput="roomId" placeholder="请输入您的管理员ID"></input>
    <text class="hint">管理员口令</text><input password="true" bindinput="roomPwd" placeholder="请输入管理员密钥"></input>
  </view>
  <view class="modal-btn-wrapper">
    <view class="cancel-btn" bindtap="cancel">取消</view>
    <view class="confirm-btn" bindtap="confirm">确定</view>
  </view>
</view>