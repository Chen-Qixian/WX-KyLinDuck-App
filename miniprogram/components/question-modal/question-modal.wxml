<view class='mask' wx:if='{{show}}' bindtap='clickMask'></view>
<view class='modal' wx:if='{{show}}'>
  <image src="../../images/component_bg.jpg" class="bg"></image>
  <view class="title">导师考核，现在开始～</view>
  <view class="question-content">
    <view class="student">
      挑战学员 <text class="badge orange">{{ansArray[student]}}</text>
    </view>
    <view class="question">{{question}}</view>
    <view class="mentor">
      灵魂导师 <text class="badge green">{{contriArray[mentor]}}</text>
    </view>
  </view>
  <view class="modal-btn-wrapper">
    <view class="cancel-btn" bindtap="cancel">取消</view>
    <view class="confirm-btn" bindtap="cancel">确定</view>
  </view>
</view>